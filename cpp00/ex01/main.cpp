#include <phonebook.hpp>
#include <contact.hpp>
#include <iostream>

int	main(int ac, char **av)
{
	std::string	input;
	int			index;
	index = 0;
	if (ac == 2)
	{
		while (1)
		{
			std::cin >> input;
			if (input == "ADD")
				
			else if (input == "SEARCH")

			else if (input == "EXIT")

		}
	}
	std:: cout << std:: endl;
}
